<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mcaadmissionhelp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".FAQ"
            android:label="@string/faqs" />
        <activity
            android:name=".College"
            android:label="Colleges" />
        <activity
            android:name=".AskQuestions"
            android:label="Ask Questions" />
        <activity android:name=".WebViewActivity" />
        <activity android:name=".AdmissionProcess" />
        <activity android:name=".ContactDTE" />
    </application>

</manifest>